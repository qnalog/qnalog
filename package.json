{
  "name": "qnalog",
  "version": "1.0.0",
  "description": "Obsidian plugin: record and transcribe conversations, then turn them into structured Markdown notes.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "typecheck:core": "tsc -p tsconfig.strict-core.json",
    "lint": "eslint src",
    "check:versions": "node scripts/check-version-alignment.mjs",
    "check:undefined-symbols": "node scripts/check-undefined-symbols.mjs",
    "build": "npm run check:versions && npm run check:undefined-symbols && npm run typecheck:core && tsc -noEmit -skipLibCheck && node esbuild.config.mjs production && node scripts/check-mobile-bundle-load.mjs",
    "install:vault": "node scripts/install-to-vault.mjs",
    "restore:vault": "node scripts/restore-from-backup.mjs",
    "test": "vitest run",
    "verify": "npm run lint && npm run build && npm test",
    "test:watch": "vitest",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "transcription",
    "voice-notes",
    "meeting-notes"
  ],
  "author": "Q&A Log Team",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@typescript-eslint/parser": "^8.67.0",
    "esbuild": "^0.28.2",
    "eslint": "^9.39.5",
    "eslint-plugin-obsidianmd": "^0.4.1",
    "obsidian": "latest",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.67.0",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "ws": "^8.21.0"
  }
}
