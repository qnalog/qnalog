|Chinese|English|
|---|---|
|本组文件较多，已显示最近 |This group has many files, showing the most recent |
|来源：|Source: |
|检测到电脑音频输入：|Computer audio input detected: |
|步骤 |Step |
|段落 · |Segments · |
|每隔多少分钟切一段，单位分钟。有效范围 0.5–30。|How often to start a new segment, in minutes. Valid range 0.5–30.|
|沉淀状态写回失败：|Failed to write back the distillation status: |
|测试失败：|Test failed: |
|热词提取失败：|Hotword extraction failed: |
|用户原文：|Original user text: |
|的姓名|'s name|
|秒|s|
|第 |#|
|等待时间已按有效范围 0–60000 毫秒调整为 |Wait time adjusted to the valid range 0–60000 milliseconds: |
|系统默认麦克风 · 可用（|System default microphone · available (|
|组织：|Organization: |
|置信度：|Confidence: |
|获取模型列表失败：|Failed to get the model list: |
|获取模型失败：|Failed to get models: |
|表格视图创建完成：新建 |Table views created: |
|覆盖 |Covering |
|角色：|Role: |
|触发类型：|Trigger type: |
|设备检测失败：|Device detection failed: |
|读到 |Detected|
|读取失败：|Failed to read: |
|读取文本文件列表失败：|Failed to read the text file list: |
|转写词表已扫描 |Transcription glossary scanned |
|返回：|Returned: |
|还差 |Still missing |
|还有 |Another |
|这里是上次扫描后尚未处理的 |These are the ones not yet processed since the last scan: |
|这里是已忽略的 |These are the ones already ignored: |
|连接失败：|Connection failed: |
|选择导入音频模型（共 |Select the audio import model (|
|选择模型（共 |Select a model (|
|重命名失败：|Rename failed: |
|重试转写${failedTasks.length > 1 ? |Retry transcription${failedTasks.length > 1 ? |
|重试转写失败片段（|Retry failed transcription segments (|
|重试这篇纪要的 |Retry this minutes note's |
|问一问失败：|Ask failed: |
|问一问需要先完成大模型配置：|Ask requires the LLM configuration to be completed first: |
|音频文件夹不存在：|Audio folder does not exist: |
|预计 |Estimated |
|（已选择）|(selected)|
|（当前匹配）|(current match)|
|（电脑音频：|(computer audio: |
|（返回：|(returned: |
|）。请在上方「麦克风」下拉中确认本人说话用的设备。|). Confirm the device you speak into in the "Microphone" dropdown above.|
|，合并 |, merged |
|，并把正文/属性里的 ${renames.map(r => |, and also correct those in the body/properties: ${renames.map(r => |
|，并把正文里的 ${hotwordRenames.map(r => |, and also correct those in the body: ${hotwordRenames.map(r => |
|，待办 |, to-dos |
|，打开对应设置|, open the corresponding settings|
|，更新 |, updated |
|，热词 |, hot words |
