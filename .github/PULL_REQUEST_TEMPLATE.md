## 改了什么 / What changed

## 为什么 / Why

## 怎么验的 / How verified

- [ ] `npm run verify:push` 通过，结果贴在此处 / pasted below
- [ ] 行为改动附带或更新了测试；bug 修复含一条修复前会失败的测试 / behavior changes come with tests; bug fixes include a test that fails before the fix
- [ ] 新增设置键同时登记 `normalizePluginSettings` 与 `serializePluginSettings`（如适用）/ new settings keys registered in both whitelists, if applicable
- [ ] 未新增上游仓库标识或更新源 / no upstream repository identifiers or update sources added
