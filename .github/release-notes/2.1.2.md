## 修复

- 修复 Obsidian 移动端（包括鸿蒙系统）因启动时加载 Node WebSocket 依赖而导致的插件加载失败。
- 将实时流式 ASR 的 Node WebSocket 改为仅在桌面端实际使用时加载；移动端继续支持分段转写和整段音频转写。
- 新增移动端构建启动检查，防止 Node、Electron、Buffer 或 process 依赖再次进入插件启动路径。
- 保留桌面端实时流式 ASR 的鉴权请求头支持。
