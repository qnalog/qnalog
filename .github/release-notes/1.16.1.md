### 修复

- 修复长录音或长文档在 AI 输出达到长度上限、可见正文为空时不再续写，导致最终纪要为空的问题。
- 截断后先关闭深度思考重试，再进行有限次数续写；仍未恢复时保留原始转写并显示明确错误，不再写入“无输出”。
- 长会议输出预算改为按录音时长和转写体量计算，不再受旧模型名称或固定全局上限限制；仅在服务端明确拒绝时自动降档。
- 补充分步诊断，区分续写尝试、成功拼接、空结果和请求失败。
- 修复通过 Obsidian 社区目录更新后，插件数据中的已安装版本仍停留在旧版本的问题。

受影响的历史纪要可直接使用“重新整理”从已保留的原始转写生成新纪要，无需重新录音。
